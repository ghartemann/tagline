<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}tagline.{% endblock %}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="icon" href="{{ asset('favicon.png') }}">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>

    <body>
        <div id="app">
            {# L'application Vue est injectée ici #}
        </div>

        <div id="modal-wrapper">
            {# Les modales seront téléportées ici #}
            {# cf. https://vuejsdevelopers.com/2020/03/16/vue-js-tutorial/#creating-a-new-vue-3-app #}
        </div>
    </body>
</html>
